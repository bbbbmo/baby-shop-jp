<!-- BEGIN:nextjs-agent-rules -->

# 이 Next.js는 기존에 알던 것과 다릅니다

이 버전은 브레이킹 체인지가 있습니다. API, 컨벤션, 파일 구조가 학습 데이터와 다를 수 있습니다. 코드를 작성하기 전에 `node_modules/next/dist/docs/`의 관련 가이드를 읽고, 디프리케이션 안내를 따르세요.

<!-- END:nextjs-agent-rules -->

# 프로젝트 개요

- 브랜드명: **COMO**
- 이 프로젝트는 **일본 소비자를 대상**으로 **여러 한국 아기옷 브랜드를 구매 대행**해 판매하는 **편집샵(멀티 브랜드 셀렉트샵)**입니다.
- 기본 사용자 언어/맥락은 일본이며, 한국어도 함께 지원합니다.

# 디자인

## 모바일 우선

- **모바일 환경을 최우선으로 고려**하세요 (mobile-first). 실제 사용자 대부분이 모바일에서 접속한다고 가정합니다.
- 좁은 화면에서 레이아웃이 깨지지 않도록 먼저 설계하고, 데스크톱은 확장 형태로 대응하세요.
- 터치 타깃 크기, 가독성, 한 손 조작 등 모바일 UX를 고려하세요.

## 비주얼 톤

- **화이트 베이스 + 무채색(그레이스케일)** 테마를 유지하세요. 배경은 `#ffffff`, 면/호버는 연한 그레이(`sand` 등).
- 색상 토큰은 `src/app/globals.css`의 CSS 변수를 사용하세요. 파스텔·세이지·블러시 등 유채색 테마로 되돌리지 마세요.
- 예외: 상품 썸네일·컬러 스와치 등 **실제 상품 색**은 그대로 표시합니다. NEW/BEST 배지는 기존 세이지/블러시 포인트(`#8c9a83` / `#d99e97`)를 유지해도 됩니다.
- CTA 등 강조 버튼은 **검정(`bg-black` / `bg-foreground`)** 을 우선하세요.

## 형태 (각진 디자인)

- **전 UI를 각지게** 유지하세요 (`border-radius: 0`). `rounded-*` / pill / 카드 둥근 모서리를 새로 쓰지 마세요.
- 전역으로 `globals.css`에서 `border-radius: 0 !important`가 적용되어 있습니다. 이를 우회하거나 둥근 UI를 추가하지 마세요.
- 상품 이미지·카테고리 타일은 **테두리 없이** 면(배경색)으로만 구분하세요.

## 레이아웃 & 여백

- 콘텐츠 컨테이너는 **`max-w-480`(1920px) + `px-6 sm:px-10`** 으로 통일하세요 (홈·목록·검색·상세·장바구니·풋터·헤더 본문).
- 헤더 배경/공지바는 풀폭이어도, **안쪽 콘텐츠 행**은 위 컨테이너와 같은 max/패딩을 쓰세요.
- 상품 그리드는 모바일 **2열**을 유지하고, 여백을 줄여 이미지를 크게 보이게 하세요 (열 수를 무작정 늘리지 마세요).

## 헤더 & 내비게이션

- 브랜드명은 헤더 **정중앙**에 크게 표시하고, 서브타이틀은 헤더에 넣지 마세요.
- 좌측: 메뉴 아이콘 + **`MENU` 텍스트**. 클릭 시 **좌측 드로어**로 카테고리·검색 내비.
- 헤더에 검색 입력바를 두지 마세요. 검색은 드로어에 둡니다.
- 메뉴·장바구니 아이콘은 **버튼 박스/호버 면 없이 아이콘(과 필요 시 텍스트)만** 보이게 하세요.

## 홈 (랜딩)

- 진입 첫 화면은 **풀뷰포트에 가까운 히어로 캐러셀**로 이벤트/정보를 보여 주세요.
- 자동 전환(약 5초), 좌우 화살표, 모바일 스와이프, 하단 도트, 호버 시 자동전환 일시정지를 유지하세요.

# 아키텍처 & 상태

## Feature-Sliced Design (FSD)를 따르세요

- FSD 레이어로 코드를 구성하세요: `app` → `pages` → `widgets` → `features` → `entities` → `shared`.
- 단방향 import만 허용합니다 (상위 레이어 → 하위 레이어. 역방향 금지).
- 슬라이스별 `index.ts` public API를 우선하고, 슬라이스 간 깊은 경로 import는 피하세요.
- 가능하면 UI, model, lib를 해당 슬라이스 안에 함께 두세요.

## Zustand는 최대한 배제하세요

- 기본적으로 새 Zustand 스토어를 만들지 마세요.
- 새 기능에는 React state (`useState` / `useReducer`), Context, URL/search params, 또는 서버 상태를 우선하세요.
- 기존 Zustand (`src/store` 등)는 마이그레이션하거나, 더 단순한 대안이 없을 때만 수정하세요.
- 전역 클라이언트 상태가 불가피하면 새 Zustand 스토어보다 얇은 Context + reducer(또는 유사 패턴)를 선호하세요.

# 문서

- `docs/superpowers/`(브레인스토밍 설계 문서 등)는 로컬 전용이며 `.gitignore`에 등록되어 있습니다. git으로 추적하거나 커밋하지 마세요.
